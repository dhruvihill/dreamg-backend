class FetchMatch {
  fetchMatches(numberOfMatchesToBeIgnored, limit, status) {
    return new Promise((resolve, reject) => {
      const matches = [];
      const matchsToBeIgnored = numberOfMatchesToBeIgnored || 0;
      const limit = limit || 10;
      const status = status || [];
    });
  }
}
